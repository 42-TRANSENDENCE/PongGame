server {
        listen 3000 default_server;
        listen [::]:3000 default_server;

        root /front_build;

        index index.html;

        server_name _;

        location / {
                try_files $uri $uri/ =404;
        }
}
